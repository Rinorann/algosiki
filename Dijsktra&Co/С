def FloydWarshall(adj_matrix):
    n = len(adj_matrix)
    for k in range(n):
        for i in range(n):
            for j in range(n):
                adj_matrix[i][j] = max(adj_matrix[i][k] + adj_matrix[k][j], adj_matrix[i][j])
    return adj_matrix